{{ config(schema="public", tags=["top-level"]) }}
-- Final base SQL model
select
    {{ adapter.quote('id') }},
    url,
    base,
    body,
    head,
    draft,
    {{ adapter.quote('state') }},
    title,
    _links,
    labels,
    {{ adapter.quote('locked') }},
    {{ adapter.quote('number') }},
    node_id,
    user_id,
    diff_url,
    html_url,
    assignees,
    closed_at,
    issue_url,
    merged_at,
    patch_url,
    auto_merge,
    created_at,
    updated_at,
    assignee_id,
    commits_url,
    comments_url,
    milestone_id,
    statuses_url,
    requested_teams,
    merge_commit_sha,
    active_lock_reason,
    author_association,
    review_comment_url,
    requested_reviewers,
    review_comments_url,
    _ab_github_repository,
    _airbyte_emitted_at,
    _airbyte_tydo_pull_requests_hashid
from {{ ref('tydo_pull_requests_ab3') }}
-- tydo_pull_requests from {{ source('public', '_airbyte_raw_tydo_pull_requests') }}

